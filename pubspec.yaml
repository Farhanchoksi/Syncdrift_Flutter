name: syncdrift_workspace
description: Monorepo workspace for Syncdrift_flutter database framework.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dev_dependencies:
  melos: ^6.0.0
